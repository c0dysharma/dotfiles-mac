[credential]
	helper = osxkeychain
[user]
	email = kuldeep.sharma@saucelab.com
	name = Kuldeep Sharma
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
